import { styled } from '@stitches/react'

export const Container = styled('div', {
  width: '100%',
  height: '100%',

  display: 'flex',
  gap: '4rem',
})

export const MyRatings = styled('section', {
  minWidth: '39rem',
})

export const DataNotFoundBox = styled('h3', {
  color: '$white',
})
